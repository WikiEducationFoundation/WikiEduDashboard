import React from 'react';

export default () => (
  <div className="example-interface-notice">
    <p>This is the same My Articles interface that students. You can use it to walk through the same steps students will go through. However, only student contributions will be tracked.</p>
  </div>
);
