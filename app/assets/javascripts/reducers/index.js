import { combineReducers } from 'redux';


const reducer = combineReducers({
  articleDetails,
  categories,
  confirm,
  didYouKnow,
  feedback,
  needHelpAlert,
  notifications,
  revisions,
  ui,
  userCourses
});

export default reducer;
