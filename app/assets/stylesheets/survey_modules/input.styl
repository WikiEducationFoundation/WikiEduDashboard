.survey-page #main
  input[type=text],
  input[type='email'],
  input[type='password'],
  textarea,
  select,
  .expandingArea > pre
    background-color $bg_light_gray
    color $text_dk
    font-size 17px
    &:focus
      border-color $contextual
