.container.form-container
  %h1 Requested accounts #{@course.slug}

  - @course.requested_accounts.each do |request|
    %li= "#{request.username} — #{request.email}"

.container
  %a.button{href: "/requested_accounts/#{@course.slug}/create", method: :post} Create these accounts
