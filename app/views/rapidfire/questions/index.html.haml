= render 'rapidfire/question_groups/admin_header'
.container.survey__edit
  .section-header
    %h1
      Editing Questions in #{@question_group.name}
    .survey__admin-tools
      = link_to "Preview Question Group", new_question_group_answer_group_path(@question_group) + '?preview'
  = render 'question_group_list'
