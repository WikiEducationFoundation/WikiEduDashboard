.container.form-container
  %h1 Requested accounts #{@campaign.slug}

  - @campaign.requested_accounts.each do |request|
    %li
      "#{request.username} — #{request.email}"&nbsp
      %a{href: "/campaigns/#{@campaign.slug}/requested_accounts/#{request.id}/delete", 'data-method': :delete}> Delete this request



.container
  %a.button{href: "/campaigns/#{@campaign.slug}/requested_accounts/create", 'data-method': :post} Create these accounts

.container
  %a.button{href: "/campaigns/#{@campaign.slug}/requested_accounts/new", 'data-method': :get} New account request
