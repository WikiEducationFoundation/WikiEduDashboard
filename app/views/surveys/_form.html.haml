
= simple_form_for(@survey) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name

  .form-input
    / = binding.pry
    = f.association :rapidfire_question_groups,   as: :check_boxes, :checked => params[:preset_id]

  .form-actions
    = f.button :submit, :class => 'button'
