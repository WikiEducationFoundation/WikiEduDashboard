<li class="row article-list__row">
  <%= link_to article_url(ac.article), :target => "_blank" do %>
  <div class="row__wrapper">
    <div class="article-list__row__rating desktop-only-flex popover-trigger">
      <p class="rating_num hidden"><%= rating_priority(ac.article.rating) %></p>
      <div class="rating <%= ac.article.rating %>"><p><%= rating_display(ac.article.rating) || '-' %></p></div>
      <div class="popover dark">
        <p><%= t("article.rating_docs.#{ac.article.rating || '-'}") %></p>
      </div>
    </div>
    <div class="article-list__row__title">
      <div class="rating <%= ac.article.rating %> tablet-only-flex"><p><%= rating_display(ac.article.rating) || '-' %></p></div>
      <p class="title"><%= full_title(ac.article) + (ac.new_article ? " (new)" : "") %></p>
    </div>
    <div class="article-list__row__title">
      <% @assignment = Assignment.find_by(course_id: ac.course_id, article_id: ac.article_id) %>
      <% unless @assignment.nil? %>
        <p class="assigned_to"><%= @assignment.user.wiki_id %></p>
      <% else %>
        <p class="assigned_to hidden">ZZZ</p>
        <p class="red"><%= t("article.no_assignee") %></p>
      <% end %>
    </div>
    <div class="article-list__row__characters">
      <p>
        <span class="tablet-only-flex"><%= t("metrics.char") %>: </span>
        <span class="characters"><%= ac.character_sum || ac.article.character_sum %></span>
      </p>
    </div>
    <div class="article-list__row__views">
      <p>
        <span class="tablet-only-flex"><%= t("metrics.view") %>: </span>
        <span class="views"><%= ac.view_count || ac.article.views || 0 %></span>
      </p>
    </div>
    <div class="article-list__row__link desktop-only-flex">
      <p class="fake-link"><%= t("article.view") %></p>
    </div>
    <div class="row__fake__link tablet-only"><p class="icon icon-arrow"></p></div>
  </div>
  <% end %>
</li>
