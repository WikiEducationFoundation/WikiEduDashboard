- content_for :before_title, t("#{Features.default_course_string_prefix}.campaign_courses", title: @campaign.title) + ' — '

= render 'nav'

.container
  %section.overview.container
    = render 'courses/header_stats', presenter: @presenter

.container
  %section#articles
    .section-header
      %h3
        = t("#{Features.default_course_string_prefix}.campaign_articles", title: @campaign.title)

    %table.table.table--hoverable.table--sortable
      %tbody.list
        - @campaign.articles.each do |article|
          %tr
            %td.title
              = article.title
