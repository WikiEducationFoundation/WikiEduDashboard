.container
  %h1 Term Recap Emails

  = form_tag('/mass_email/term_recap/send') do
    = label_tag :campaign
    = select_tag 'campaign', options_from_collection_for_select(Campaign.all, 'id', 'title'), prompt: 'Choose campaign'
    .button
      = button_tag 'Send recap emails'
