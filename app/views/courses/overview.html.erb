<% content_for :before_title do "#{@course.title} - Overview - " end %>

<%= render :partial => 'header', :locals => {:page => 0} %>

<div class="course_main">
  <section id="react_root" class="overview container">
    <!-- This is where React takes over -- See /app/javascripts for relevant code -->
  </section>
</div>