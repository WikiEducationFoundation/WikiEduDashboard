.container
  %h1 Alerts

  %table.list-basic
    %tr
      %th Date/Time
      %th Type
      %th Course
      %th User
    - @alerts.each do |alert|
      %tr
        %td= alert.created_at
        %td= alert.type
        %td= link_to(alert.course.slug, "/courses/#{alert.course.slug}") if alert.course
        %td= alert.user&.username
