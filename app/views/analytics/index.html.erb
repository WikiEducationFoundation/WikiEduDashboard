<% content_for :after_title do " - Analytics" end %>
<header class="main-page">
  <div class="container">
    <h1>Analytics Tools</h1>
  </div>
</header>
<div class="container">
<section>
<div class="section-header">
<h3><%= form_tag(method: 'get') do %>
  <input name="monthly_report" type="hidden" value="true" />
  <button class="button dark"><%= submit_tag("Monthly Report") %></button>

  <!-- <%= label_tag(:category, "category") %>
  <%= text_field_tag(:category, @category) %><br/>
  <%= label_tag(:depth, "depth") %>
  <%= text_field_tag(:depth) %><br/>
  <%= label_tag(:minviews, "minimum views") %>
  <%= text_field_tag(:minviews, @min_views) %><br/>
  <%= label_tag(:maxwp10, "maximum completeness (0-100)") %>
  <%= text_field_tag(:maxwp10, @max_wp10) %><br/>
  <button class="button dark"><%= submit_tag("Go") %></button> -->
<% end %><br/>
<% if params[:monthly_report] %>
  <%= render :partial => 'monthly_report', :locals => { :monthly_report => @monthly_report } %>
<% end %>
</h3>

<h3><%= form_tag(method: 'get') do %>
  <input name="cohort_stats" type="hidden" value="true" />
  <button class="button dark"><%= submit_tag("Cohort stats") %></button>
<% end %><br/>
<% if params[:cohort_stats] %>
  <%= render :partial => 'cohort_stats', :locals => { :cohort_stats => @cohort_stats } %>
<% end %>
</h3>


</div>
</section>
</div>
